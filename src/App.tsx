import React, { useEffect, useState } from 'react'; import MainWindow from './ui/MainWindow/MainWindow'; import { RavenCore } from './core/RavenCore'; import { MemoryManager } from './memory/MemoryManager'; export default function App() { const [ravenCore, setRavenCore] = useState(null); const [memoryManager, setMemoryManager] = useState(null); useEffect(() => {const mem = new MemoryManager(); const core = new RavenCore(mem); setMemoryManager(mem); setRavenCore(core);}, []); if (!ravenCore || !memoryManager) return <div className="flex items-center justify-center h-screen bg-slate-900 text-white">Loading Raven...</div>; return <MainWindow ravenCore={ravenCore} memoryManager={memoryManager} />;};