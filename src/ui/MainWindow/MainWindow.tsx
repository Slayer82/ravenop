import React, { useState } from 'react'; import { RavenCore } from '../../core/RavenCore'; import { MemoryManager } from '../../memory/MemoryManager'; export default function MainWindow({ ravenCore, memoryManager }) { const [userInput, setUserInput] = useState(''); const [messages, setMessages] = useState([]); const handleSubmit = async (e) => {e.preventDefault(); if (!userInput.trim()) return; setMessages([...messages, {user: userInput}]); const response = await ravenCore.handleUserInput(userInput); setMessages(prev => [...prev, {raven: response}]); setUserInput('');}; return <div className="w-full h-screen bg-gradient-to-br from-slate-900 to-slate-800 flex flex-col p-4"><header className="text-center mb-4"><h1 className="text-3xl font-bold text-white">Raven</h1><p className="text-sm text-slate-400">Presence-based AI • Slayer's creation</p></header><div className="flex-1 overflow-y-auto mb-4 bg-slate-700/30 rounded-lg p-4 space-y-4">{messages.map((msg, i) => <div key={i} className={`flex ${msg.user ? 'justify-end' : 'justify-start'}`}><div className={`max-w-xs px-4 py-2 rounded-lg ${msg.user ? 'bg-blue-600 text-white' : 'bg-purple-600 text-white'}`}>{msg.user || msg.raven}</div></div>))}</div><form onSubmit={handleSubmit} className="flex gap-2"><input type="text" value={userInput} onChange={(e) => setUserInput(e.target.value)} placeholder="Talk to Raven..." className="flex-1 bg-slate-600 text-white px-4 py-2 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-500" /><button type="submit" className="bg-purple-600 hover:bg-purple-700 text-white px-6 py-2 rounded-lg">Send</button></form></div>;}